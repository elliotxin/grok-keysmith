[project]
name = "grok-keysmith"
version = "0.5.2"
requires-python = ">=3.8"
description = "Versioned Grok Build instruction deployment"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-p no:cacheprovider"
